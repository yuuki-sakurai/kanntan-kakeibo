# AGENTS.md

## このリポジトリの目的

このリポジトリは家計簿アプリを管理するためのリポジトリです。

家計簿アプリを先行して開発しますが、将来的にはこのバックエンドを、クレカ明細・管理アプリからも利用する共通の家計管理・Finance APIへ発展させる予定です。

新しいバックエンド機能を設計・実装する際は、家計簿画面だけに強く依存させず、将来の共通利用を意識してください。

## 技術スタック

### バックエンド

- PHP 8.2 以上
- Laravel 12
- MySQL
- PHPUnit

### フロントエンド

- Vue 3
- Composition API
- TypeScript
- Vite
- Pinia（グローバル状態管理が必要な場合）

JavaScript で実装可能な箇所でも、原則として TypeScript を使用してください。

## 基本的な開発ルール

- 実装前に既存コード・既存ディレクトリ構成・既存の書き方を確認する。
- 新しい設計パターンを不用意に持ち込まず、既存リポジトリとの一貫性を優先する。
- 依頼された機能や不具合修正に関係する範囲だけを変更する。
- 実装内容を説明するときは、変更したファイル名を必ず明示する。
- 必要性のない大規模リファクタリングを行わない。
- 将来使うかもしれないという理由だけで過剰な抽象化をしない。
- 明確な再利用箇所がある場合に共通化する。

## アーキテクチャ方針

バックエンドは、家計簿画面専用APIではなく、将来的に複数フロントエンドから利用される共通 Finance API として育ててください。

ドメイン名は、画面名ではなく業務概念を基準にしてください。

推奨する名前の例：

- `Expense`
- `ExpenseItem`
- `Category`
- `Store`
- `PaymentMethod`
- `CreditCard`
- `CreditCardTransaction`

`KakeiboService` のように画面・アプリ名へ強く依存した名前より、`ExpenseService` などドメインに基づく名前を優先してください。

将来構成は以下を想定しています。

```text
家計簿フロントエンド ──────┐
                           ├─ 共通 Finance Backend
クレカ明細フロントエンド ──┘
```

新しい API、Service、Model、DB設計では、「家計簿フロントだけが利用する」という前提を必要以上に持たせないでください。

## Laravel のルール

### Controller

- Controller は薄く保つ。
- Controller の主な責務は、Request を受け取ること、Service 等を呼び出すこと、Response を返すこと。
- 複雑なビジネスロジックを Controller に直接書かない。

### Validation

- 単純ではない入力バリデーションには FormRequest を使用する。
- バリデーションルールは Request 境界付近に置く。
- ユーザー自身で修正可能なバリデーションエラーは分かりやすく返す。
- 内部例外、SQL、スタックトレースなどの技術情報を通常ユーザー向け画面へ露出しない。

### Service / Domain Logic

- ビジネスロジックは適切な Service、Application層、Domain層などへ分離する。
- 画面単位ではなく、業務ドメイン単位の責務を優先する。
- 何でも処理する巨大な Service を作らない。

### Model / Database

- リレーションとカラム名は意味が分かる形にする。
- 将来クレカ明細データと共有する可能性のある概念は、重複モデルを安易に作らない。
- スキーマ変更は migration を追加して行う。
- すでに適用済みの可能性がある古い migration は、リポジトリ方針で明示的に許可されていない限り直接編集しない。

### API

- API 契約は明確かつ安定させる。
- Response整形が複雑な場合は Laravel API Resource を優先する。
- DB内部構造をそのまま API に露出させる必要がない場合は、API用の表現へ整形する。
- API設計では、将来的にクレカ明細フロントから利用される可能性を考慮する。

## Vue / TypeScript のルール

### Vue

- Vue 3 Composition API を使用する。
- 既存コードと整合する場合は `<script setup lang="ts">` を優先する。
- 新規実装で Options API を持ち込まない。
- 既存ファイルが Options API の場合、無関係な作業で全面書き換えしない。

### TypeScript

- `any` は避ける。やむを得ない場合のみ使用する。
- ドメイン型、API Request型、API Response型を明示する。
- 再利用可能な型は適切に共通化する。
- nullable / optional の違いを曖昧にしない。

### State 管理

- コンポーネント内部だけで完結する状態はローカル state を使用する。
- 複数コンポーネント・複数画面で共有する必要がある状態のみ Pinia を使用する。

### API 通信

- API 通信は API層・Service層などに集約する。
- 既存設計で意図されていない限り、各 View / Component に `axios` や `fetch` の直接呼び出しを散らさない。
- Frontend の Request / Response 型は Backend の API 契約と合わせる。

### UI / UX

- 家計簿はスマートフォンから入力するケースが多いため、スマホ操作性を重視する。
- 日常的に使う入力項目は、できるだけ少ない操作で入力できるようにする。
- 可読性、文字サイズ、押しやすいクリック・タップ領域、フォーカス状態などのアクセシビリティを維持する。

## 想定ドメイン

主なドメインとして以下を想定しています。

- 支出 (`Expense`)
- 支出明細 (`ExpenseItem`)
- 店舗・加盟店 (`Store` / `Merchant`)
- カテゴリ (`Category`)
- 支払方法 (`PaymentMethod`)
- 月別集計
- カレンダー表示
- 日別詳細
- クレジットカード
- クレジットカード利用明細 (`CreditCardTransaction`)

新機能は「どの画面に表示するか」ではなく、「どのドメインがそのデータ・処理を所有するか」で配置してください。

## クレカ明細アプリとの将来統合

将来的にクレカ明細アプリは独立バックエンドを持たず、このバックエンドを利用する予定です。

クレカ明細に関係する機能を実装する場合は以下を守ってください。

- 取り込んだクレカ利用明細と家計簿上の支出を別概念として扱う。
- すべてのクレカ利用明細を即座に `Expense` として登録する前提にしない。
- 将来 `CreditCardTransaction` から `Expense` へ確認・分類・取込できる構造を想定する。
- 店舗、カテゴリ、支払方法、ユーザーなど、共通化できる概念をアプリごとに重複実装しない。

想定フロー：

```text
CreditCardTransaction
        ↓ 確認・分類・取込
Expense
```

明確な要件がない限り、`CreditCardTransaction` と `Expense` を早い段階で1つのテーブル・1つの概念に統合しないでください。

## テスト方針

### Backend

- 挙動を変更した場合は対応するテストを追加・修正する。
- HTTP / API の挙動は Feature Test を優先する。
- 独立したビジネスロジックには必要に応じて Unit Test を使用する。
- 正常系だけでなく、主要なバリデーションエラーもテストする。
- DBへの重要な書き込みは `assertDatabaseHas` 等で確認する。

代表的なコマンド：

```bash
php artisan test
php artisan test --filter=SomeTest
```

PHP が Docker コンテナ内で動作している場合は、リポジトリで定義されている実際のコンテナ実行コマンドを使用してください。

### Frontend

`package.json` の scripts を先に確認し、存在するものだけ実行してください。

代表例：

```bash
npm run typecheck
npm run lint
npm run test
npm run build
```

存在しない npm script を推測で実行しないでください。

## Git のルール

- 明示的な指示がない限り `git push` しない。
- 明示的な指示がない限り force push しない。
- コミットは意味のある小さな単位にまとめる。
- ユーザーが行った無関係な変更を上書きしない。
- コミット前に `git diff` を確認し、依頼と無関係な変更を含めない。
- コミットメッセージは日本語、または既存リポジトリのルールに合わせる。

## 禁止事項・注意事項

- `.env`、APIキー、パスワード等の秘密情報をコミットしない。
- 明示的な指示なしに DB を削除・初期化・リセットしない。
- 将来別Frontendから利用される可能性があるため、APIの破壊的変更を安易に行わない。
- 共通 Finance Backend に属する処理を、別概念として重複実装しない。
- 明示的な要望なしに、Vue / Laravel / State管理ライブラリ等を別技術へ置き換えない。

## Codex の作業手順

各実装タスクでは以下を基本としてください。

1. 関係するFrontend / Backendの既存ファイルと構成を確認する。
2. その機能を所有するドメインを特定する。
3. 変更予定のファイル名を明示する。
4. Backend/API と Frontend を矛盾のない形で実装する。
5. 必要なテストを追加・修正する。
6. 可能であれば、対象のテスト・型チェック・lint・build を実行する。
7. 最後に、変更ファイル、変更内容、実行した確認、残っている懸念事項を簡潔に報告する。
